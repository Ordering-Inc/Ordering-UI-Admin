import styled from 'styled-components'

export const Container = styled.div`
  height: calc(100% - 130px);
  > button {
    position: sticky;
    top: 100%;
    margin-top: 30px;
    height: 44px;
  }
`
