import React from 'react'
import { DriversLogs as DriversLogsController } from 'ordering-components-admin'

export const DriversLogs = (props) => {
  return (
    <DriversLogsController {...props} />
  )
}
